---
phase: 01-portfolio-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.jsx
autonomous: true
requirements:
  - PE-01
  - PE-02

must_haves:
  truths:
    - "Hero section has terminal/code aesthetic without breaking existing layout"
    - "Skills cards have developer-focused structure with code-like elements"
    - "All existing components and imports remain unchanged"
    - "Background remains unchanged"
  artifacts:
    - path: "src/App.jsx"
      provides: "Enhanced portfolio structure"
      min_lines: 500
  key_links:
    - from: "Hero section"
      to: "Terminal aesthetic"
      via: "Code-style borders and monospace fonts"
    - from: "Skills cards"
      to: "Developer structure"
      via: "Terminal window styling"
---

<objective>
Enhance the portfolio's hero and skills sections with a developer-focused, terminal/code aesthetic while preserving all existing functionality, imports, and the particle background.

Purpose: Transform the portfolio from a standard React app to a polished developer portfolio with terminal-inspired UI patterns.
Output: Enhanced hero section with terminal styling, improved skills cards with code-like structure
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/App.jsx
@src/components/Navbar.jsx
@src/components/Particeles.jsx
</context>

<read_first>
- `src/App.jsx` — Current hero structure, skills mapping, all imports (do NOT remove any imports)
- `src/index.css` — Existing Tailwind configuration
- `tailwind.config.js` — Theme colors and fonts
</read_first>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Enhance Hero Section with Terminal Aesthetic</name>
  <files>src/App.jsx</files>
  <action>
    **CRITICAL: Do NOT modify any imports at the top of App.jsx. Only modify the JSX in the return statement.**

    In the Hero Element section (name="Home"), enhance the structure:

    1. **Add terminal-style header bar** to the left content block:
       - Add a div with class `bg-slate-800/60 border border-slate-700/50 rounded-t-xl p-2 px-4 flex items-center gap-2`
       - Include three dots: red (`bg-red-500`), yellow (`bg-yellow-500`), green (`bg-green-500`) circles
       - Add monospace title: `<span class="text-slate-400 text-xs font-mono ml-2">~/portfolio</span>`

    2. **Wrap the main hero content in terminal window styling**:
       - Add class `bg-slate-900/80 border border-slate-700/50 rounded-b-xl p-6 font-mono text-sm`
       - Convert intro text to look like terminal output with `$` prefixes
       - Keep existing text content but add monospace styling

    3. **Style the name with ASCII-art or code-block aesthetic**:
       - Wrap in `bg-slate-800/40 border-l-2 border-indigo-500 pl-4`
       - Use gradient text as before but add code comment style prefix

    4. **Enhance the profile image section**:
       - Add terminal-style frame: `border-2 border-slate-700` with `border-t-indigo-500`
       - Add corner decorations using absolute positioned elements
       - Keep the existing gradient glow and animation

    5. **Style CTA buttons with command button aesthetic**:
       - Add `border border-slate-600 hover:border-indigo-500 font-mono text-sm`
       - Make secondary button look like `--secondary` flag

    KEEP EXACTLY AS IS:
    - All imports at the top
    - The profile image src and styling
    - The Particles background
    - The gradient text colors
    - All existing component props and behaviors
  </action>
  <verify>
    <automated>grep -c "terminal\|terminal-window\|font-mono\|rounded-t-xl\|rounded-b-xl" src/App.jsx</automated>
  </verify>
  <done>
    Hero section has terminal aesthetic with window chrome, monospace fonts, and code-style borders while preserving all existing content and the particle background.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Enhance Skills Cards with Developer Structure</name>
  <files>src/App.jsx</files>
  <action>
    **CRITICAL: Do NOT modify imports. Only modify the skills mapping rendering.**

    In the Skills Element section, enhance the skill category cards:

    1. **Restructure each skill category card** to look like a terminal window:
       - Add header bar: `bg-slate-800/80 border-b border-slate-700/50 rounded-t-xl px-4 py-2`
       - Add terminal dots (smaller: w-2 h-2) in the header
       - Add file path: `<span class="text-slate-500 text-xs font-mono">skills/${category.title.toLowerCase()}.json</span>`

    2. **Enhance the category title area**:
       - Remove the large letter avatar
       - Replace with terminal prompt icon: `<span class="text-indigo-400 font-mono">$</span>`
       - Style title with `font-mono text-indigo-300`

    3. **Improve skill item grid**:
       - Each item should look like a package/dependency
       - Add `bg-slate-900/50 border border-slate-700/30 rounded-lg p-2`
       - Show icon as before but with terminal-style background
       - Add code-style brackets around name: `[${item.name}]`

    4. **Add terminal-style metadata**:
       - Below header, add line count indicator: `<!-- ${category.items.length} items -->`
       - Style with `text-slate-600 text-xs font-mono`

    5. **Maintain all existing Tilt effects and hover animations**:
       - Keep `react-parallax-tilt` wrapper
       - Keep all existing hover transitions

    KEEP EXACTLY AS IS:
    - The skills data array (skills variable)
    - The icon image paths
    - The Tilt component wrapper
    - All existing animations
    - The responsive grid breakpoints
  </action>
  <verify>
    <automated>grep -c "skills/\|font-mono\|\$\|items\]" src/App.jsx</automated>
  </verify>
  <done>
    Skills cards styled as terminal windows with file paths, monospace fonts, and code-like structure while preserving all tilt effects and data.
  </done>
</task>

</tasks>

<verification>
- All imports remain unchanged in App.jsx
- Background particles component still renders
- All existing animations preserved (tilt, hover, gradients)
- No functionality removed from any component
- Terminal aesthetic applied consistently across hero and skills sections
</verification>

<success_criteria>
1. Hero section has terminal window chrome with dots and path bar
2. Hero content uses monospace font styling
3. Profile image has code-block frame styling
4. Skills section cards look like terminal windows
5. All existing imports preserved exactly
6. Particles background unchanged
7. No existing components removed from imports
</success_criteria>

<output>
After completion, create `.planning/phases/01-portfolio-enhancement/01-01-SUMMARY.md`
</output>
