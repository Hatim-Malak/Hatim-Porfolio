---
phase: 01-portfolio-enhancement
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - src/App.jsx
autonomous: true
requirements:
  - PE-05

must_haves:
  truths:
    - "Contact form has terminal/REPL input styling"
    - "Social links styled as command icons"
    - "Footer has minimal terminal aesthetic"
    - "Entire portfolio has cohesive developer theme"
  artifacts:
    - path: "src/App.jsx"
      provides: "Complete enhanced portfolio with cohesive developer theme"
      min_lines: 500
  key_links:
    - from: "Contact form"
      to: "Terminal REPL"
      via: "Command-style inputs"
    - from: "Social links"
      to: "Terminal actions"
      via: "Command button styling"
---

<objective>
Polish the contact section with terminal/REPL aesthetics and ensure footer has consistent styling. Final touches to create a cohesive developer portfolio theme across all sections.

Purpose: Complete the developer portfolio transformation with terminal-style contact form and cohesive footer.
Output: Contact form as terminal interface, polished footer, complete portfolio theme
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/App.jsx
</context>

<read_first>
- `src/App.jsx` — Contact section, Footer component integration
- Previous plans: `.planning/phases/01-portfolio-enhancement/01-01-PLAN.md`, `01-02-PLAN.md`
</read_first>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Style Contact Form as Terminal REPL Interface</name>
  <files>src/App.jsx</files>
  <action>
    **CRITICAL: Do NOT modify the form logic, emailjs integration, or imports. Only modify JSX styling.**

    In the Contact Element section (name="Contact"):

    1. **Enhance section header** with terminal prompt style:
       - Keep existing "Contact Me" title
       - Add terminal window chrome above form
       - Add prompt: `text-indigo-400 font-mono">contact.sh --init</span>`

    2. **Wrap entire form in terminal window styling**:
       - Container: `bg-slate-900/80 border border-slate-700/50 rounded-xl overflow-hidden`
       - Add terminal header bar with dots

    3. **Style each input as command line input**:
       - Each input wrapper: `bg-slate-800/40 border-b border-slate-700/50 p-3`
       - Add command prompt before each input:
         - Name field: `text-indigo-400 font-mono mr-2">$ name:</span>`
         - Email field: `text-indigo-400 font-mono mr-2">$ email:</span>`
         - Message field: `text-indigo-400 font-mono mr-2">$ message:</span>`
       - Style inputs: `bg-transparent border-none text-white font-mono text-sm w-full focus:outline-none placeholder:text-slate-600`
       - Add blinking cursor effect on focus: `caret-indigo-400`

    4. **Style textarea** similarly with prompt prefix

    5. **Style submit button as terminal command**:
       - Change text to command format: `./send_message.sh`
       - Add terminal-style: `bg-slate-800 border border-slate-600 font-mono text-indigo-300`
       - Keep gradient on hover but add terminal cursor blink

    6. **Enhance success/error states**:
       - Success toast: keep `toast.success` but styling already handled
       - Consider adding console-style prefix: `[SUCCESS]` or `[ERROR]`

    KEEP EXACTLY AS IS:
    - All form ref and handlers (useRef, sendEmail function)
    - emailjs integration and configuration
    - All input name attributes
    - All existing toast notifications
    - Loading state handling
  </action>
  <verify>
    <automated>grep -c "send_message\|font-mono\|command\|\\\$" src/App.jsx</automated>
  </verify>
  <done>
    Contact form styled as terminal REPL with command-line inputs, prompts, and terminal-styled submit button while preserving all form functionality.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Style Social Links as Terminal Command Buttons</name>
  <files>src/App.jsx</files>
  <action>
    **CRITICAL: Do NOT modify href links or icon imports. Only modify styling.**

    In the social links section (the flex gap-6 mt-12 div with Github, Linkedin, Mail links):

    1. **Add terminal command wrapper**:
       - Container: `bg-slate-900/60 border border-slate-700/50 rounded-xl p-4 flex items-center gap-4`
       - Add label: `text-slate-400 text-sm font-mono mr-2">connect:</span>`

    2. **Style each social link as terminal command**:
       - Each link wrapper: `p-3 bg-slate-800/60 border border-slate-700/50 rounded-lg hover:border-indigo-500/50 transition-all`
       - Keep existing Lucide icons (Github, Linkedin, Mail)
       - Add command-style background on hover: `hover:bg-indigo-500/10`

    3. **Add decorative command text** below icons:
       - On hover, show small label like:
         - `Github` → `[git remote add origin]`
         - `Linkedin` → `[network connect]`
         - `Mail` → `[smtp send]`
       - Style: `text-slate-600 text-xs font-mono absolute -bottom-6 left-1/2 -translate-x-1/2 opacity-0 group-hover:opacity-100 transition-opacity`

    4. **Wrap icons in group for hover effects**:
       - Add `group relative` to each anchor tag

    KEEP EXACTLY AS IS:
    - All href links
    - All icon imports
    - The existing hover transitions (already have hover:border-indigo-500)
    - The spacing and positioning
  </action>
  <verify>
    <automated>grep -c "connect:\|command\|group relative" src/App.jsx</automated>
  </verify>
  <done>
    Social links styled as terminal command buttons with hover labels while preserving all existing links and icons.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Polish Footer and Final Theme Cohesion</name>
  <files>src/App.jsx</files>
  <action>
    **CRITICAL: Do NOT modify Footer component. Only modify how it's imported and used.**

    1. **Add terminal footer styling**:
       - Wrap Footer import in a styled container: `bg-slate-900/80 border-t border-slate-700/50 mt-12`
       - Add decorative terminal line above: `h-px bg-gradient-to-r from-transparent via-slate-700 to-transparent`

    2. **Ensure consistent font usage**:
       - If Footer component has text, it should use `font-mono text-slate-500 text-sm`

    3. **Add global polish - ensure all sections have consistent terminal styling**:
       - Verify each section header has consistent styling:
         - All section titles: `text-white text-4xl md:text-5xl font-bold font-mono`
         - All subtitles: `text-indigo-400 text-sm font-mono uppercase tracking-widest`
         - All decorative lines: `h-px bg-gradient-to-r from-indigo-500 via-purple-500 to-indigo-500`

    4. **Add subtle code-style decorations**:
       - At the very top of the main content (after Particles): Add thin gradient line
       - Between major sections: Add `// section break` style comments if space allows

    5. **Final visual check points**:
       - All rounded-xl borders should use slate-700/50
       - All monospace text should use slate-400 for secondary, white/indigo for primary
       - All gradients should remain from-indigo-500 to-purple-500

    KEEP EXACTLY AS IS:
    - Footer component functionality
    - All existing animations and effects
    - The overall dark theme
    - All existing component behaviors
  </action>
  <verify>
    <automated>grep -c "font-mono\|border-slate-700\|gradient-to-r from-indigo" src/App.jsx</automated>
  </verify>
  <done>
    Footer polished with terminal styling, global theme consistency achieved across all sections with monospace fonts and code-style decorations.
  </done>
</task>

</tasks>

<verification>
- Contact form maintains all emailjs functionality
- Social links preserve all href attributes
- Footer component unchanged
- All animations and effects preserved
- Consistent developer theme across entire portfolio
</verification>

<success_criteria>
1. Contact form inputs have command-line prompts
2. Submit button styled as terminal command
3. Social links have command-style hover labels
4. Footer has terminal footer styling
5. Consistent monospace fonts throughout
6. Global theme cohesive - terminal/developer aesthetic applied to all sections
7. All existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/01-portfolio-enhancement/01-03-SUMMARY.md`
</output>
