---
phase: 01-portfolio-enhancement
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/App.jsx
autonomous: true
requirements:
  - PE-03
  - PE-04

must_haves:
  truths:
    - "Projects section has terminal/code editor styling"
    - "Education timeline looks like git commit history"
    - "All existing project data and interactions preserved"
    - "Education content unchanged but styled uniquely"
  artifacts:
    - path: "src/App.jsx"
      provides: "Enhanced projects and education sections"
      min_lines: 500
  key_links:
    - from: "Projects cards"
      to: "Terminal windows"
      via: "Code editor frame styling"
    - from: "Education entries"
      to: "Git history"
      via: "Commit-style timeline"
---

<objective>
Enhance the projects and education sections with developer-focused aesthetics - projects styled as terminal/code editor windows, education as git commit history.

Purpose: Create a cohesive developer portfolio feel where each section uses different but complementary code/terminal patterns.
Output: Terminal-styled project cards with code metadata, git history-styled education timeline
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/App.jsx
</context>

<read_first>
- `src/App.jsx` — Current projects and education sections (lines 250+)
- Previous plan summary: `.planning/phases/01-portfolio-enhancement/01-01-SUMMARY.md` (if exists)
</read_first>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Style Projects Section as Code Editor Windows</name>
  <files>src/App.jsx</files>
  <action>
    **CRITICAL: Do NOT modify imports or projects data array. Only modify JSX rendering.**

    In the Projects Element section (name="Projects"):

    1. **Enhance section header** with code editor styling:
       - Keep existing "Featured Projects" title
       - Add terminal-style bar above: `bg-slate-800/60 rounded-t-xl border border-slate-700/50 p-2 flex items-center gap-2`
       - Terminal dots (red/yellow/green) + `text-slate-500 text-xs font-mono ml-2">projects/</span>`

    2. **Restructure project cards to look like code files**:
       - Each collapsed card gets:
         - `bg-slate-900/80 border border-slate-700/50 rounded-xl overflow-hidden`
         - File tab at top: `bg-slate-800/60 px-3 py-1 text-indigo-400 text-xs font-mono border-b border-slate-700/50`
         - File name: `${pro.name}.{js|jsx|json}` with appropriate extension
       - Keep existing image area and description structure
       - Add terminal-style skill badges with brackets: `[$SKILL]`

    3. **Enhance the expanded project modal**:
       - Add code editor chrome: header bar with file tabs
       - Add line numbers gutter on left side (decorative)
       - Style with `bg-slate-900 border border-slate-700 font-mono text-sm`
       - Keep existing functionality (close button, links)

    4. **Add code-like metadata below project descriptions**:
       - Add: `// ${pro.skills.length} technologies used`
       - Style: `text-slate-500 text-xs font-mono ml-4`

    KEEP EXACTLY AS IS:
    - All project data (name, description, img, skills, code, live links)
    - Click handlers (setopenpro)
    - All modal functionality
    - All existing animations
    - The responsive layout
  </action>
  <verify>
    <automated>grep -c "projects/\|font-mono\|border border-slate-700" src/App.jsx</automated>
  </verify>
  <done>
    Projects section styled as code editor windows with file tabs, terminal chrome, and code-like metadata while preserving all existing data and interactions.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Style Education Timeline as Git Commit History</name>
  <files>src/App.jsx</files>
  <action>
    **CRITICAL: Do NOT modify education data array. Only modify JSX rendering.**

    In the Education Element section:

    1. **Enhance section header** with git-style styling:
       - Add `font-mono text-slate-400` to header text
       - Add git branch decoration: vertical line now styled as git history

    2. **Transform each education entry to look like a git commit**:
       - Entry container: `bg-slate-900/60 border border-slate-700/50 rounded-lg p-4 font-mono`
       
       - Add commit header line: `commit ${edu.id.toString(16).padStart(7, '0')} --author="Education"`
         - Style: `text-indigo-400 text-xs bg-slate-800/80 px-2 py-1 rounded inline-block mb-2`
       
       - Add date as git-style: `Date: ${edu.date}`
         - Style: `text-slate-500 text-xs mb-3`
       
       - Keep the school/degree info but add commit message styling:
         - Degree as commit message: `text-white font-semibold`
         - School as author: `text-slate-400 text-sm`
       
       - Style grade as git tag: `[${edu.grade}]`
         - Add `bg-indigo-500/20 text-indigo-300 px-2 py-0.5 rounded text-xs`
       
       - Add decorative git diff lines (decorative):
         - `+ ${edu.desc.substring(0, 50)}...`
         - Style: `text-slate-600 text-xs mt-3`

    3. **Enhance the timeline line**:
       - Keep existing vertical line but style more prominent
       - Add commit dots with hash-style: `bg-indigo-500` circles

    KEEP EXACTLY AS IS:
    - All education data (school, degree, date, grade, desc, img)
    - The alternating left/right layout on desktop
    - The responsive mobile layout
    - All existing hover effects
    - Image display in cards
  </action>
  <verify>
    <automated>grep -c "commit \|Date:\|font-mono" src/App.jsx</automated>
  </verify>
  <done>
    Education timeline styled as git commit history with hash-style commits, date stamps, and diff decorations while preserving all content and layout.
  </done>
</task>

</tasks>

<verification>
- All project data arrays unchanged
- All education data unchanged
- Click handlers for project modal still work
- Responsive layouts preserved
- Git history aesthetic applied to education
- Code editor aesthetic applied to projects
</verification>

<success_criteria>
1. Projects section has code editor window styling with file tabs
2. Each project card has file extension in tab
3. Education entries styled as git commits with hash, date, message
4. Timeline line enhanced to look like git history
5. All data arrays unchanged
6. Modal functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/01-portfolio-enhancement/01-02-SUMMARY.md`
</output>
